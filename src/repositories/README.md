# Repositories

This directory is reserved for repository pattern classes if database query abstractions are introduced. Currently, queries are handled directly via Prisma inside Server Actions for optimal performance.
